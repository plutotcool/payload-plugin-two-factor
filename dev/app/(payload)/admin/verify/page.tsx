// The 2FA verification step. In a consumer app you would import this from
// `@plutotcool/payload-plugin-two-factor/client`; the dev app reads it from
// `src` so changes are picked up without a rebuild.
export { TwoFactorVerifyPage as default } from '../../../../../src/exports/client'
